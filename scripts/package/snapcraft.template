name: kernel
version: KERNELRELEASE
summary: freax kernel
description: The upstream freax kernel
grade: stable
confinement: strict
type: kernel

parts:
  kernel:
    plugin: kernel
    source: SRCTREE
    source-type: tar
    kernel-with-firmware: false
